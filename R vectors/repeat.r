# repeat data in vector using rep() function.

repeat_data<-rep(c(1,2,3,4),each=4)   # each element repeat 4 times

repeat_data



#now,,, 

repeat_data<-rep(c(1,2,3,4),times=c(2,1,5,4))  # whole element's set is repeated 4 times
repeat_data
